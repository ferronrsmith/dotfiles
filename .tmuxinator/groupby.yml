# ~/.tmuxinator/groupby.yml

name: groupby
root: ~/

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu
# use the `tmux list-windows` command to retrieve the exact layout you have configured as shown below
windows:
  - editor:
      layout: 9821,226x54,0,0{113x54,0,0,0,112x54,114,0,10}
      panes:
        - vim
        - #empty
  - dev:
      layout: 2f9b,226x54,0,0{113x54,0,0,1,112x54,114,0,9}
      panes:
        - #empty
        - #empty
  - server:
      layout: 8bbf,226x54,0,0[226x36,0,0,2,226x17,0,37,6]
      panes:
        - htop
        - sudo nethogs wlan0
  - entertainment:
      layout: 7fe3,226x54,0,0{113x54,0,0,3,112x54,114,0[112x27,114,0,7,112x26,114,28,8]}
      panes:
        - #empty
        - mp3blaster
        - #empty

